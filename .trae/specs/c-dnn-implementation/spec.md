# 规格说明（当前实现基线）

## 目标

将网络配置统一为**图拓扑描述**，并通过 profiler 生成可执行 spec，供训练和推理共享。

## 范围

- 配置输入：`config_user.h` 图节点/边定义
- 图处理：校验、拓扑排序、规格构建
- 执行入口：训练/推理仅接受 `NetworkSpec`
- 文档与 demo 与测试统一到新流程

## 不在范围

- 旧接口兼容层
- 模型类型二选一开关

## 核心约束

- 图是单一事实来源
- 同一 spec 用于训练与推理
- 所有示例和测试必须通过新配置链路运行
