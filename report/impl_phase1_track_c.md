# 轨 C — 构建系统配置化

**关联**: [implementation_steps.md](implementation_steps.md) (总索引)
**前置**: 阶段 0 (P0 + P0.5 已完成)
**可并行**: 与轨 A、轨 B 并行推进
**对应根因**: RC4 (构建为脚本非配置)

> 负责人 C 独立推进。轨 C 完成前不依赖轨 A 和轨 B 的输出。

---

## 步骤 C1 — 创建 cmake/compiler_flags.cmake

### 代码变更

**新建**: `cmake/compiler_flags.cmake`

```cmake
# ─── 单一真实来源: 编译器标志 ───
# 本文件被所有 CMakeLists.txt include()

function(action_c_apply_strict_warnings TARGET_NAME)
    if(MSVC)
        target_compile_options(${TARGET_NAME} PRIVATE /W4 /WX)
    else()
        target_compile_options(${TARGET_NAME} PRIVATE
            -Wall -Wextra -Wpedantic -Werror
        )
    endif()
endfunction()

function(action_c_apply_hardening TARGET_NAME)
    if(MSVC)
        target_compile_options(${TARGET_NAME} PRIVATE
            /GS /guard:cf /DYNAMICBASE /sdl
        )
        target_link_options(${TARGET_NAME} PRIVATE
            /HIGHENTROPYVA /NXCOMPAT
        )
    else()
        target_compile_options(${TARGET_NAME} PRIVATE
            -D_FORTIFY_SOURCE=2
            -fstack-protector-strong
            -fPIE -fPIC
        )
        target_link_options(${TARGET_NAME} PRIVATE
            -Wl,-z,relro -Wl,-z,now -pie
        )
    endif()
endfunction()

function(action_c_enable_lto TARGET_NAME)
    include(CheckIPOSupported)
    check_ipo_supported(RESULT ipo_supported OUTPUT ipo_output)
    if(ipo_supported)
        set_property(TARGET ${TARGET_NAME}
            PROPERTY INTERPROCEDURAL_OPTIMIZATION TRUE)
    endif()
endfunction()

function(action_c_apply_all_flags TARGET_NAME)
    action_c_apply_strict_warnings(${TARGET_NAME})
    action_c_apply_hardening(${TARGET_NAME})
    action_c_enable_lto(${TARGET_NAME})
endfunction()
```

### 步骤测试

**测试文件**: `tests/build/test_compiler_flags.cmake`

CMake 级别的测试用 `cmake -P` 脚本验证：

```cmake
# tests/build/test_compiler_flags.cmake
# 测试 compiler_flags.cmake 可被 include 且函数定义不报错

include("${CMAKE_SOURCE_DIR}/cmake/compiler_flags.cmake")
message(STATUS "compiler_flags.cmake loaded successfully")
```

**测试用例**:

| # | 测试 | 命令 | 预期 |
|---|------|------|------|
| 1 | cmake 模块语法 | `cmake -P cmake/compiler_flags.cmake` | 无错误 |
| 2 | 函数可调用 | `cmake -P tests/build/test_compiler_flags.cmake` | `loaded successfully` |
| 3 | strict_warnings 生成正确标志 | 手动检查 `cmake --build` 输出含 `-Wall -Wextra` | 包含 |

### 单步验证

```bash
# 语法检查
cmake -P cmake/compiler_flags.cmake

# 测试脚本
cmake -DSOURCE_DIR=. -P tests/build/test_compiler_flags.cmake
```

### 全量回归

```bash
# 确保现有构建不受影响（compiler_flags.cmake 尚未被 include）
cmake --build build/ --config Debug 2>&1 | grep -E "error|warning"
```

### 门控

- [x] cmake 模块语法通过
- [x] 现有构建不受影响
- [ ] → **git commit** `"feat(C1): add cmake/compiler_flags.cmake module"`
- [ ] → 进入 C2

---

## 步骤 C2 — 创建 cmake/version.cmake

### 代码变更

**新建**: `cmake/version.cmake`

```cmake
# ─── 单一真实来源: 版本号 ───
set(ACTION_C_VERSION_MAJOR 1)
set(ACTION_C_VERSION_MINOR 0)
set(ACTION_C_VERSION_PATCH 0)
set(ACTION_C_VERSION "${ACTION_C_VERSION_MAJOR}.${ACTION_C_VERSION_MINOR}.${ACTION_C_VERSION_PATCH}")

function(action_c_generate_version_header OUT_DIR)
    set(VERSION_HEADER "${OUT_DIR}/action_c_version.h")
    file(WRITE "${VERSION_HEADER}"
        "/* Auto-generated by cmake/version.cmake. DO NOT EDIT. */\n"
        "#ifndef ACTION_C_VERSION_H\n"
        "#define ACTION_C_VERSION_H\n"
        "#define ACTION_C_VERSION_MAJOR ${ACTION_C_VERSION_MAJOR}\n"
        "#define ACTION_C_VERSION_MINOR ${ACTION_C_VERSION_MINOR}\n"
        "#define ACTION_C_VERSION_PATCH ${ACTION_C_VERSION_PATCH}\n"
        "#define ACTION_C_VERSION_STRING \"${ACTION_C_VERSION}\"\n"
        "#endif\n"
    )
endfunction()
```

### 步骤测试

**测试用例**:

| # | 测试 | 命令 | 预期 |
|---|------|------|------|
| 1 | cmake 模块语法 | `cmake -P cmake/version.cmake` | 无错误 |
| 2 | 版本号生成 | `cmake -P tests/build/test_version.cmake` | 生成正确的 version.h |

**测试文件**: `tests/build/test_version.cmake`

```cmake
include("${CMAKE_SOURCE_DIR}/cmake/version.cmake")

# 验证版本变量
if(NOT ACTION_C_VERSION_MAJOR EQUAL 1)
    message(FATAL_ERROR "MAJOR version mismatch")
endif()

# 测试头文件生成
set(TEST_DIR "${CMAKE_BINARY_DIR}/test_version_output")
file(MAKE_DIRECTORY "${TEST_DIR}")
action_c_generate_version_header("${TEST_DIR}")

file(READ "${TEST_DIR}/action_c_version.h" header_content)
if(NOT header_content MATCHES "1\\.0\\.0")
    message(FATAL_ERROR "Generated version string mismatch")
endif()
message(STATUS "version.cmake: all checks passed")
```

### 单步验证

```bash
cmake -P cmake/version.cmake
cmake -DSOURCE_DIR=. -P tests/build/test_version.cmake
```

### 全量回归

```bash
cmake --build build/ --config Debug 2>&1 | grep -E "error|warning"
```

### 门控

- [x] version.cmake 语法通过
- [x] 版本头生成正确
- [ ] → **git commit** `"feat(C2): add cmake/version.cmake with header generation"`
- [ ] → 进入 C3

---

## 步骤 C3 — 重写 src/CMakeLists.txt 使用 cmake/ 模块

### 代码变更

**修改**: `src/CMakeLists.txt`

删除本文件中定义的 `action_c_apply_strict_warnings` 函数。添加：
```cmake
include("${CMAKE_SOURCE_DIR}/cmake/compiler_flags.cmake")
include("${CMAKE_SOURCE_DIR}/cmake/version.cmake")
```

将所有 `action_c_apply_strict_warnings(target)` 调用改为 `action_c_apply_all_flags(target)`。

**修改**: `demo/demo_common.cmake` — 删除函数定义，改为 include：
```cmake
include("${ACTION_C_ROOT}/cmake/compiler_flags.cmake")
```

### 步骤测试

**测试用例**:

| # | 测试 | 命令 | 预期 |
|---|------|------|------|
| 1 | CMake 配置 | `cmake -B build/ -S .` | 成功，无重复定义警告 |
| 2 | 全量编译 | `cmake --build build/ --config Debug 2>&1` | 零错误 |
| 3 | 编译标志包含 hardening | `cmake --build build/ --config Debug --verbose 2>&1` | 含 `-fstack-protector-strong` (非 MSVC) |
| 4 | 编译标志含 strict | `cmake --build build/ --verbose 2>&1` | 含 `-Wall -Wextra -Werror` (非 MSVC) |

### 单步验证

```bash
# 清除旧构建缓存
rm -rf build/
cmake -B build/ -S .
cmake --build build/ --config Debug 2>&1 | grep -E "error|warning"
```

### 全量回归

```bash
ctest --output-on-failure
bash scripts/run_demo.sh move all
```

### 门控

- [x] CMake 配置成功 (无重复定义)
- [x] 全量编译零错误零警告
- [x] 编译标志正确（含 hardening）
- [x] move demo 正常
- [ ] → **git commit** `"refactor(C3): migrate src/ and demo/ to use cmake/ modules"`
- [ ] → 进入 C4

**消灭问题**: STB-H02, STB-M11

---

## 步骤 C4 — 修复 file(GLOB) 添加 CONFIGURE_DEPENDS

### 代码变更

**修改**: `src/nn/CMakeLists.txt`

```cmake
# 旧
file(GLOB _infer_entry "${CMAKE_CURRENT_SOURCE_DIR}/types/${TYPE_NAME}/nn_type_*_infer.c")

# 新
file(GLOB _infer_entry CONFIGURE_DEPENDS
    "${CMAKE_CURRENT_SOURCE_DIR}/types/${TYPE_NAME}/nn_type_*_infer.c")
```

对所有 GLOB 调用添加 `CONFIGURE_DEPENDS`。

### 步骤测试

**测试用例**:

| # | 测试 | 操作 | 预期 |
|---|------|------|------|
| 1 | CMake 配置 | `cmake -B build/ -S .` | 成功 |
| 2 | 全量编译 | `cmake --build build/` | 零错误 |
| 3 | 重配置触发 | 在 `src/nn/types/` 下新增一个 `dummy_test_glob.c` | CMake 自动重配置 |

### 单步验证

```bash
cmake -B build/ -S .
cmake --build build/ --config Debug 2>&1 | grep -E "error|warning"
```

### 全量回归

```bash
ctest --output-on-failure
```

### 门控

- [x] CMake 不再有 "GLOB without CONFIGURE_DEPENDS" 警告
- [x] 全量编译通过
- [ ] → **git commit** `"fix(C4): add CONFIGURE_DEPENDS to all file(GLOB) calls"`
- [ ] → 进入 C5

**消灭问题**: STB-H01

---

## 步骤 C5 — 为所有 demo 添加构建依赖

### 代码变更

**修改**: 每个 demo 的 `train/CMakeLists.txt`

```cmake
add_dependencies(${DEMO_NAME}_train ${DEMO_NAME}_generate)
```

**修改**: 每个 demo 的 `infer/CMakeLists.txt`

```cmake
add_dependencies(${DEMO_NAME}_infer ${DEMO_NAME}_generate)
```

### 步骤测试

**测试用例**:

| # | 测试 | 命令 | 预期 |
|---|------|------|------|
| 1 | 构建 train 触发 generate | `cmake --build build/ --target move_train` | 先构建 `move_generate` |
| 2 | 构建 infer 触发 generate | `cmake --build build/ --target move_infer` | 先构建 `move_generate` |
| 3 | 构建所有 demo | `cmake --build build/` | 零错误 |

### 单步验证

```bash
# 清除构建
cmake --build build/ --target clean 2>/dev/null || true

# 仅构建 train——应自动先构建 generate
cmake --build build/ --target move_train --config Debug 2>&1 | head -20
```

### 全量回归

```bash
cmake --build build/ --config Debug 2>&1 | grep -E "error|warning"
ctest --output-on-failure
bash scripts/run_demo.sh move all
```

### 门控

- [x] 构建 train 自动触发 generate
- [x] 全量编译通过
- [ ] → **git commit** `"fix(C5): add demo build dependencies (train→generate, infer→generate)"`
- [ ] → 进入 C6

**消灭问题**: STB-H04

---

## 步骤 C6 — 修复 demo_common.cmake 路径计算

### 代码变更

**修改**: `demo/demo_common.cmake` — `action_c_get_demo_generated_dir`

```cmake
function(action_c_get_demo_generated_dir OUT_VAR)
    if(DEFINED ACTION_C_GENERATED_DIR)
        set(${OUT_VAR} "${ACTION_C_GENERATED_DIR}" PARENT_SCOPE)
    else()
        get_filename_component(_gen_dir "${CMAKE_BINARY_DIR}/data" ABSOLUTE)
        set(${OUT_VAR} "${_gen_dir}" PARENT_SCOPE)
    endif()
endfunction()
```

**修改**: 每个 demo 的 `generate/CMakeLists.txt`

```cmake
set(ACTION_C_GENERATED_DIR "${CMAKE_BINARY_DIR}/data"
    CACHE INTERNAL "Generated code output directory")
```

### 步骤测试

**测试用例**:

| # | 测试 | 命令 | 预期 |
|---|------|------|------|
| 1 | 自定义构建目录配置 | `cmake -B build/custom -S demo/move/generate` | 成功 |
| 2 | 自定义目录构建 | `cmake --build build/custom` | 成功 |
| 3 | 生成文件路径正确 | `ls build/custom/data/` | 包含生成的 `.h`/`.c` 文件 |

### 单步验证

```bash
cmake -B build/path_test -S demo/move/generate -DCMAKE_BUILD_TYPE=Debug
cmake --build build/path_test --config Debug
ls build/path_test/data/
```

### 全量回归

```bash
# 标准构建
cmake -B build/ -S .
cmake --build build/ --config Debug 2>&1 | grep -E "error|warning"
bash scripts/run_demo.sh move all
```

### 门控

- [x] 自定义构建目录成功
- [x] 生成文件路径正确
- [x] 标准构建 + move demo 正常
- [ ] → **git commit** `"fix(C6): fix demo_common.cmake path calculation for arbitrary build dirs"`
- [ ] → 进入 C7

**消灭问题**: STB-H03

---

## 步骤 C7 — 创建 CMakePresets.json

### 代码变更

**新建**: `CMakePresets.json` (项目根目录)

```json
{
  "version": 6,
  "configurePresets": [
    {
      "name": "debug",
      "displayName": "Debug",
      "binaryDir": "${sourceDir}/build/debug",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_C_STANDARD": "11",
        "CMAKE_C_STANDARD_REQUIRED": "ON",
        "CMAKE_C_EXTENSIONS": "OFF"
      }
    },
    {
      "name": "release",
      "displayName": "Release",
      "binaryDir": "${sourceDir}/build/release",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Release",
        "CMAKE_C_STANDARD": "11",
        "CMAKE_C_STANDARD_REQUIRED": "ON",
        "CMAKE_C_EXTENSIONS": "OFF"
      }
    },
    {
      "name": "hardened",
      "displayName": "Hardened Release",
      "inherits": "release",
      "binaryDir": "${sourceDir}/build/hardened"
    },
    {
      "name": "sanitized",
      "displayName": "Sanitized Debug",
      "inherits": "debug",
      "binaryDir": "${sourceDir}/build/sanitized",
      "cacheVariables": {
        "CMAKE_C_FLAGS": "-fsanitize=address,undefined -fno-omit-frame-pointer"
      }
    }
  ],
  "buildPresets": [
    {
      "name": "debug",
      "configurePreset": "debug",
      "jobs": 0
    },
    {
      "name": "release",
      "configurePreset": "release",
      "jobs": 0
    },
    {
      "name": "hardened",
      "configurePreset": "hardened",
      "jobs": 0
    },
    {
      "name": "sanitized",
      "configurePreset": "sanitized",
      "jobs": 0
    }
  ]
}
```

### 步骤测试

**测试用例**:

| # | 测试 | 命令 | 预期 |
|---|------|------|------|
| 1 | 列出预设 | `cmake --list-presets` | 显示 4 个预设 |
| 2 | debug 配置 | `cmake --preset debug` | 成功 |
| 3 | release 配置 | `cmake --preset release` | 成功 |
| 4 | hardened 配置 | `cmake --preset hardened` | 成功 |
| 5 | sanitized 配置 | `cmake --preset sanitized` | 成功（需要 ASan 支持） |
| 6 | debug 构建 | `cmake --build --preset debug` | 零错误 |
| 7 | release 构建 | `cmake --build --preset release` | 零错误 |

### 单步验证

```bash
cmake --list-presets
cmake --preset debug
cmake --build build/debug --config Debug 2>&1 | tail -5

cmake --preset release
cmake --build build/release --config Release 2>&1 | tail -5
```

### 全量回归

```bash
# 在 debug preset 下运行测试
cd build/debug && ctest --output-on-failure
bash scripts/run_demo.sh move all
```

### 门控

- [x] 4 个 preset 均列出
- [x] debug + release preset 配置和构建成功
- [x] ctest 全量通过
- [x] move demo 正常
- [ ] → **git commit** `"feat(C7): add CMakePresets.json (debug, release, hardened, sanitized)"`
- [ ] → 进入 C8

**消灭问题**: STB-H05, STB-H06, SEC-H01

---

## 步骤 C8 — 统一所有 demo CMakeLists.txt 的 C_EXTENSIONS

### 代码变更

**批量修改**: 每个 demo 的 `generate/CMakeLists.txt`、`train/CMakeLists.txt`、`infer/CMakeLists.txt`

```cmake
set(CMAKE_C_EXTENSIONS OFF)
```

由于 CMakePresets.json 中已设置，如果所有构建走 preset，此行可省略。但为独立构建兼容性，保留在每个 CMakeLists.txt 中。

### 步骤测试

**测试用例**:

| # | 测试 | 命令 | 预期 |
|---|------|------|------|
| 1 | debug preset 配置 | `cmake --preset debug` | 成功 |
| 2 | 全量编译 | `cmake --build build/debug --config Debug 2>&1` | 零警告零错误 |
| 3 | 确认 C_EXTENSIONS=OFF | 检查 `build/debug/CMakeCache.txt` | `CMAKE_C_EXTENSIONS:BOOL=OFF` |

### 单步验证

```bash
cmake --preset debug
cmake --build build/debug --config Debug 2>&1 | grep -i "error\|warning"
grep CMAKE_C_EXTENSIONS build/debug/CMakeCache.txt
```

### 门控

- [x] C_EXTENSIONS=OFF 已生效
- [x] 全量编译零错误零警告
- [ ] → **git commit** `"fix(C8): enforce CMAKE_C_EXTENSIONS=OFF in all demo CMakeLists"`
- [ ] → 进入 C9

**消灭问题**: USA-M02

---

## 步骤 C9 — 修复 Wasm CMakeLists.txt

### 代码变更

**修改**: `wasm/CMakeLists.txt`

从 `wasm_exports.c` 自动提取导出列表，使用 version.cmake 版本号：

```cmake
include("${CMAKE_SOURCE_DIR}/cmake/version.cmake")
target_compile_definitions(action_c_wasm PRIVATE
    ACTION_C_WASM_VERSION="${ACTION_C_VERSION}"
)
```

**删除**: `wasm/config/wasm_config.h` 中的硬编码版本号。

### 步骤测试

**测试用例**:

| # | 测试 | 命令 | 预期 |
|---|------|------|------|
| 1 | emcmake 配置 | `emcmake cmake -B build/wasm -S wasm/` | 成功 |
| 2 | wasm 构建 | `cmake --build build/wasm` | 成功，输出 .wasm |
| 3 | 版本定义正确 | 检查构建日志 | version 来自 cmake/version.cmake |
| 4 | 导出函数列表匹配 | 对比 .wasm exports 和 wasm_exports.c | 一致 |

### 单步验证

```bash
# 需 Emscripten 环境
emcmake cmake -B build/wasm -S wasm/
cmake --build build/wasm --config Release 2>&1 | tail -10
```

### 全量回归

```bash
# 本地构建不受影响
cmake --preset debug
cmake --build build/debug --config Debug 2>&1 | grep -E "error|warning"
```

### 门控

- [x] emcmake 配置成功
- [x] wasm 构建成功
- [ ] → **git commit** `"fix(C9): repair Wasm CMakeLists.txt (auto-export, version from cmake/)"`
- [ ] → 进入 C10

**消灭问题**: STB-C01, USA-H01, FUN-H04, FUN-M10

---

## 步骤 C10 — 修复 build_wasm.sh

### 代码变更

**修改**: `wasm/scripts/build_wasm.sh`

```bash
# 旧（有 bug——\$EMSDK 是字面字符串）
"-DCMAKE_TOOLCHAIN_FILE=\$EMSDK/upstream/emscripten/cmake/Modules/Platform/Emscripten.cmake"

# 新
if [ -z "${EMSDK}" ]; then
    echo "Error: EMSDK is not set. Run 'source emsdk_env.sh' first."
    exit 1
fi
"-DCMAKE_TOOLCHAIN_FILE=${EMSDK}/upstream/emscripten/cmake/Modules/Platform/Emscripten.cmake"
```

**修改**: `wasm/scripts/build_wasm.ps1`

```powershell
if (-not $env:EMSDK) {
    Write-Error "EMSDK not set"
    exit 1
}
```

**修改**: 构建输出目录改为项目根 `build/wasm`。

### 步骤测试

**测试用例**:

| # | 测试 | 命令 | 预期 |
|---|------|------|------|
| 1 | 无 EMSDK 时脚本报错 | `unset EMSDK; bash wasm/scripts/build_wasm.sh` | 报错 "EMSDK not set" |
| 2 | 构建成功 | `export EMSDK=...; bash wasm/scripts/build_wasm.sh` | 构建成功 |
| 3 | 构建产物在正确位置 | `ls build/wasm/*.wasm` | 存在 .wasm 文件 |

### 单步验证

```bash
# 测试错误检测
bash -c 'unset EMSDK; bash wasm/scripts/build_wasm.sh' 2>&1 | grep "EMSDK"
```

### 全量回归

```bash
cmake --preset debug
cmake --build build/debug --config Debug 2>&1 | grep -E "error|warning"
```

### 门控

- [x] 无 EMSDK 时报错（非静默失败）
- [x] 有 EMSDK 时构建成功
- [ ] → **git commit** `"fix(C10): fix build_wasm.sh EMSDK variable expansion and add error check"`
- [ ] → 进入 C11

**消灭问题**: FUN-C02, SEC-H04, STB-M13, STB-M14

---

## 步骤 C11 — 消除 CS 工具硬编码 VS 路径

### 代码变更

**修改**: `demo/cs/tools/CMakeLists.txt`

```cmake
# 旧: 硬编码
set(VCVARS "C:/Program Files/Microsoft Visual Studio/2022/Community/VC/Auxiliary/Build/vcvars32.bat")

# 新: CMake 自动检测
if(MSVC)
    message(STATUS "Using MSVC: ${CMAKE_GENERATOR}")
elseif(CMAKE_C_COMPILER_ID MATCHES "Clang")
    find_program(CLANG_CL clang-cl REQUIRED)
endif()

# 非 Windows 平台跳过 probe
if(NOT WIN32)
    message(WARNING "CS probe not available on this platform")
    return()
endif()
```

### 步骤测试

**测试用例**:

| # | 测试 | 命令 | 预期 |
|---|------|------|------|
| 1 | 有 VS 时 CMake 配置 | `cmake -B build/cs -S demo/cs/tools/` | 成功 |
| 2 | 无 VS 时 CMake 不崩溃 | 在 Linux 上 `cmake -B build/cs -S demo/cs/tools/` | 警告但不崩溃 |

### 单步验证

```bash
cmake -B build/cs_test -S demo/cs/tools/ 2>&1 | head -10
```

### 门控

- [x] CMake 配置不因路径硬编码而崩溃
- [ ] → **git commit** `"fix(C11): remove hardcoded VS paths from CS tools CMakeLists"`
- [ ] → 进入 C12

**消灭问题**: SEC-H02, SEC-H03, FUN-H05

---

## 步骤 C12 — 重写 build_demos.ps1

### 代码变更

**重写**: `build_demos.ps1`

```powershell
$demoList = @("move", "sevenseg", "target", "transformer",
              "mnist", "mnist_cnn", "nested_nav", "road_graph_nav",
              "cnn_rnn_react", "hybrid_route")

$failed = @()
foreach ($demo in $demoList) {
    Write-Host "=== Building $demo ==="
    cmake --preset debug -S "demo/$demo/generate" -B "build/demo/$demo/generate"
    if ($LASTEXITCODE -ne 0) { $failed += $demo; continue }
    cmake --build "build/demo/$demo/generate" --config Debug
    if ($LASTEXITCODE -ne 0) { $failed += "$demo (build)"; continue }
    Write-Host "$demo generate: OK" -ForegroundColor Green
}

if ($failed.Count -gt 0) {
    Write-Host "FAILED: $failed" -ForegroundColor Red
    exit 1
}
Write-Host "All demos built successfully" -ForegroundColor Green
```

### 步骤测试

**测试用例**:

| # | 测试 | 命令 | 预期 |
|---|------|------|------|
| 1 | 构建所有 demo generate | `.\build_demos.ps1` | 全部成功 |
| 2 | 单 demo 失败不影响后续 | 模拟单个 demo 源文件有错 | 继续构建其余 |

### 单步验证

```powershell
.\build_demos.ps1
```

### 门控

- [x] 所有 demo generate 阶段构建成功
- [ ] → **git commit** `"refactor(C12): rewrite build_demos.ps1 with error tracking"`
- [ ] → 进入 C13

**消灭问题**: STB-H06

---

## 步骤 C13 — 统一 demo 运行脚本平台检测

### 代码变更

**批量修改**: 所有 `demo/*/run_demo.sh`

```bash
case "$(uname -s)" in
    MINGW*|MSYS*|CYGWIN*) EXE=".exe" ;;
    *)                    EXE="" ;;
esac

"$BUILD_ROOT/generate/Debug/${DEMO}_generate${EXE}"
```

### 步骤测试

**测试用例**:

| # | 测试 | 平台 | 命令 | 预期 |
|---|------|------|------|------|
| 1 | Linux 运行 | Linux | `bash scripts/run_demo.sh move all` | 成功 |
| 2 | MINGW 运行 | Git Bash | `bash scripts/run_demo.sh move all` | 成功 |
| 3 | WSL 运行 | WSL | `bash scripts/run_demo.sh move all` | 成功 |
| 4 | 平台检测输出 | — | `echo "$(uname -s)"` | 显示当前平台 |

### 单步验证

```bash
# 当前平台测试
bash scripts/run_demo.sh move all
bash scripts/run_demo.sh sevenseg all
bash scripts/run_demo.sh target all
```

### 全量回归

```bash
# 所有 demo 脚本
for demo in move sevenseg target transformer mnist mnist_cnn \
            nested_nav road_graph_nav cnn_rnn_react hybrid_route; do
    bash scripts/run_demo.sh "$demo" all 2>&1 | tail -1
done
```

### 门控

- [x] 当前平台所有 demo 正常
- [ ] → **git commit** `"fix(C13): add platform detection to all demo run scripts"`
- [ ] → 轨 C 完成

**消灭问题**: STB-H05

---

## 轨 C 完成检查

- [x] C1: `cmake/compiler_flags.cmake`
- [x] C2: `cmake/version.cmake`
- [x] C3: src/ 和 demo/ 使用 cmake/ 模块
- [x] C4: `CONFIGURE_DEPENDS`
- [x] C5: demo 构建依赖
- [x] C6: 路径计算修复
- [x] C7: `CMakePresets.json`
- [x] C8: `CMAKE_C_EXTENSIONS OFF` 统一
- [x] C9: wasm CMakeLists.txt 修复
- [x] C10: build_wasm.sh 修复
- [x] C11: CS 工具路径修复
- [x] C12: build_demos.ps1 重写
- [x] C13: demo 脚本平台检测

**轨 C 特点**: 多为构建系统变更，测试以 CMake 配置 + 编译成功为核心，辅以 demo 脚本的跨平台验证。
**消灭问题**: RC4 (构建配置化) 共约 35 个问题。
